<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linear_layout_chat"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="left"
    android:orientation="vertical"
    android:paddingBottom="5dp"
    android:paddingStart="10dp"
    android:paddingEnd="10dp">
<net.phpsm.simsim.simsiminstantorder.CustomViews.CustomTextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/date"
    android:text=""
    android:gravity="center"
    android:textSize="@dimen/contentSize"
    android:textColor="@color/bold_text_message"
    android:padding="5dp"/>
    <net.phpsm.simsim.simsiminstantorder.CustomViews.CustomTextView
        android:id="@+id/message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/shapebackgroundmessage"
        android:gravity="start|center"
        android:maxWidth="@dimen/maxSizeTextMessage"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="7dp"
        android:paddingBottom="7dp"
        android:text="نص الرسالة"
        android:textColor="@color/white"
        android:textSize="@dimen/content" />

</LinearLayout>